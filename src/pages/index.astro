---
// Astro requires a root index page to exist so it can turn it into the
// `/` -> `/en/` redirect at build time (see `i18n.routing.prefixDefaultLocale`
// in astro.config.mjs). Its content below is never actually served; Astro
// replaces this page with the redirect during the build.
---
